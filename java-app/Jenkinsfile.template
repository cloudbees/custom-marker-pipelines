pipeline {

  agent {
    kubernetes
    {
    label 'java-app'
    }
   }
  stages {
    stage('Test') {
      parameters {
        string(defaultValue: "", description: 'Cucumber Tag', name: 'CUCUMBER_TAG')
      }
      steps {
     container('maven') {
       echo "${params.CUCUMBER_TAG}"
        sh "mvn test"
        }
      }
    }
  }
}
